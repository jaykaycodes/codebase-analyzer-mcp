node_modules/
# Keep dist/*.js in git for GitHub plugin installs
# Exclude compiled binaries (too large)
dist/cba
dist/cba-*
# Local MCP config (plugin uses .claude-plugin/plugin.json instead)
.mcp.json
*.log
.env
.env.local
.DS_Store
.claude/settings.local.json
docs/plans/
